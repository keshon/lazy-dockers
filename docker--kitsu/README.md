A modified version of https://github.com/cgwire/cgwire

Changes:
- build stage is separated from deploy
- change version in env effectivly changes build image (caution at this step as image may not build properly)