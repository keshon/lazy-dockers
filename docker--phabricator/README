After deploying update `data/app/conf/local/local.json` file:
 - update `phabricator.base-uri` to real domain without `https://`
 - update `metamta.default-address` to real email address (check it has `@***.**` part)